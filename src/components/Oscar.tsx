/* React component type assignment done by  */

type OscarProps = {
  children: React.ReactNode;
};
const Oscar = (props: OscarProps) => {
  return <div>Oscar goes to Leonado Dicpario</div>;
};

export default Oscar;
